import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'apesito.example.app',
  appName: 'Apesito',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
